import { Image } from "./image";
import { ObjectValue } from "./objectValue";

export class HomePageInfor {
    title?: string;
    position?: string;
    lstImage?: Image[];
    pageResSql?: ObjectValue[];
    id?: number;
}